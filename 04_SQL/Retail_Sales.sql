--SELECT COUNT(*)
--FROM RETAIL_SALES;
--SELECT *
--FROM RETAIL_SALES;
--DESC RETAIL_SALES;
--SELECT SUM(SALES_AMOUNT) AS TOTAL_SALES
--FROM RETAIL_SALES;
--SELECT SUM(PROFIT) AS TOTAL_PROFIT
--FROM RETAIL_SALES;
--SELECT ROUND(AVG(PROFIT_MARHIN_PCT),2) AS AVG_MARGIN
--FROM RETAIL_SALES;
--SELECT column_name
--FROM user_tab_columns
--WHERE table_name = 'RETAIL_SALES';
--SELECT COUNT(DISTINCT ORDER_ID)
--FROM RETAIL_SALES;
--SELECT PRODUCT,
--       SUM(SALES_AMOUNT) AS SALES
--FROM RETAIL_SALES
--GROUP BY PRODUCT
--ORDER BY SALES DESC
--FETCH FIRST 10 ROWS ONLY;
--SELECT BRAND,
--       SUM(SALES_AMOUNT) AS SALES
--FROM RETAIL_SALES
--GROUP BY BRAND
--ORDER BY SALES DESC
--FETCH FIRST 10 ROWS ONLY;
--SELECT CATEGORY,
--       SUM(SALES_AMOUNT) AS SALES
--FROM RETAIL_SALES
--GROUP BY CATEGORY
--ORDER BY SALES DESC;
--SELECT STATE,
--       SUM(SALES_AMOUNT) AS SALES
--FROM RETAIL_SALES
--GROUP BY STATE
--ORDER BY SALES DESC;
--SELECT GENDER,
--       SUM(SALES_AMOUNT) AS SALES
--FROM RETAIL_SALES
--GROUP BY GENDER;
--SELECT PAYMENT_MODE,
--       COUNT(*) AS ORDERS,
--       SUM(SALES_AMOUNT) AS SALES
--FROM RETAIL_SALES
--GROUP BY PAYMENT_MODE
--ORDER BY SALES DESC;
--SELECT CUSTOMER_SEGMENT,
--       SUM(SALES_AMOUNT) AS SALES,
--       SUM(PROFIT) AS PROFIT
--FROM RETAIL_SALES
--GROUP BY CUSTOMER_SEGMENT;
--SELECT TO_CHAR(ORDER_DATE,'MON-YYYY') AS MONTH,
--       SUM(SALES_AMOUNT) AS SALES
--FROM RETAIL_SALES
--GROUP BY TO_CHAR(ORDER_DATE,'MON-YYYY')
--ORDER BY MIN(ORDER_DATE);